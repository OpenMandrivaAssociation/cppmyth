--- cppmyth-2.20.4.orig/cppmyth/src/private/os/threads/atomic.h	2026-08-02 16:36:06.291855746 +0200
+++ cppmyth-2.20.4/cppmyth/src/private/os/threads/atomic.h	2026-08-02 16:36:06.297563919 +0200
@@ -351,6 +351,20 @@
       while (__new != __old);
       __val = __old + amount;
 
+#elif defined __aarch64__
+      /* aarch64 must not use 32-bit ARM (ARMv7) atomics; check before __ARM_ARCH */
+      unsigned long tmp;
+      __asm__ volatile (
+        "    dmb     ish\n"           /* Memory barrier */
+        "1:  ldxr    %w0, %2\n"
+        "    add     %w0, %w0, %w3\n"
+        "    stlxr   %w1, %w0, %2\n"
+        "    cbnz    %w1, 1b\n"
+        "    dmb     ish\n"           /* Memory barrier */
+        : "=&r" (__val), "=&r" (tmp), "+Q" (m_val)
+        : "Ir" (amount)
+        : "memory");
+
 #elif (defined __ARM_ARCH && __ARM_ARCH == 7)
       __asm__ volatile (
         "    dmb     ish\n"           /* Memory barrier */
@@ -394,19 +408,6 @@
         : "r" (&m_val), "r" (amount)
         : "cc", "memory");
 
-#elif defined __aarch64__
-      unsigned long tmp;
-      __asm__ volatile (
-        "    dmb     ish\n"           /* Memory barrier */
-        "1:  ldxr    %w0, %2\n"
-        "    add     %w0, %w0, %w3\n"
-        "    stlxr   %w1, %w0, %2\n"
-        "    cbnz    %w1, 1b\n"
-        "    dmb     ish\n"           /* Memory barrier */
-        : "=&r" (__val), "=&r" (tmp), "+Q" (m_val)
-        : "Ir" (amount)
-        : "memory");
-
 #else
 #error Atomic add are not.
 #endif
@@ -472,6 +473,20 @@
       while (__new != __old);
       __val = __old - amount;
 
+#elif defined __aarch64__
+      /* aarch64 must not use 32-bit ARM (ARMv7) atomics; check before __ARM_ARCH */
+      unsigned long tmp;
+      __asm__ volatile (
+        "    dmb     ish\n"           /* Memory barrier */
+        "1:  ldxr    %w0, %2\n"
+        "    sub     %w0, %w0, %w3\n"
+        "    stlxr   %w1, %w0, %2\n"
+        "    cbnz    %w1, 1b\n"
+        "    dmb     ish\n"           /* Memory barrier */
+        : "=&r" (__val), "=&r" (tmp), "+Q" (m_val)
+        : "Ir" (amount)
+        : "memory");
+
 #elif (defined __ARM_ARCH && __ARM_ARCH == 7)
       __asm__ volatile (
         "    dmb     ish\n"           /* Memory barrier */
@@ -515,19 +530,6 @@
         : "r" (&m_val), "r" (-amount)
         : "cc", "memory");
 
-#elif defined __aarch64__
-      unsigned long tmp;
-      __asm__ volatile (
-        "    dmb     ish\n"           /* Memory barrier */
-        "1:  ldxr    %w0, %2\n"
-        "    sub     %w0, %w0, %w3\n"
-        "    stlxr   %w1, %w0, %2\n"
-        "    cbnz    %w1, 1b\n"
-        "    dmb     ish\n"           /* Memory barrier */
-        : "=&r" (__val), "=&r" (tmp), "+Q" (m_val)
-        : "Ir" (amount)
-        : "memory");
-
 #else
 #error Atomic sub are not.
 #endif
